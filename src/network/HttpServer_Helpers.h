/**
 * @file HttpServer_Helpers.h（文件名）
 * @brief HttpServer 辅助函数声明
 * 
 * 本文件包含 HttpServer 的辅助函数声明，用于支持各个功能模块
 */

#ifndef HTTPSERVER_HELPERS_H
#define HTTPSERVER_HELPERS_H

#include <string>
#include <json/json.h>

class HttpRequest;
class HttpResponse;

// 注意：这些函数是 HttpServer 的成员函数，需要在 HttpServer 类中声明
// 此文件仅作为文档说明，实际声明在 HttpServer.h 中

#endif // 结束 HTTPSERVER_HELPERS_H


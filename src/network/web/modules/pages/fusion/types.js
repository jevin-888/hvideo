export const TABS = Object.freeze({
    GEOMETRY: 'geometry',
    MASK: 'mask',
    BLEND: 'blend',
    COLOR: 'color'
});

export const FOCUS_MODES = Object.freeze({
    LAYOUT: 'layout'
});

export const BLEND_SIDES = Object.freeze(['left', 'right', 'top', 'bottom']);

export const BLEND_SIDE_LABELS = Object.freeze({
    left: '左',
    right: '右',
    top: '上',
    bottom: '下'
});

